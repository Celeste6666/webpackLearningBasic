import '$css/main';
import {plus,multiple} from '@/js/calculate';

console.log('index plus', _.add(3, 2));
console.log('index multiple', _.multiply(2, 4));

console.log('calculate plus', plus(5, 2));
console.log('calculate multiple',multiple(6, 4));